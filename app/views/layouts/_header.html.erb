<nav class="navbar navbar-expand-md bg-info">
  <div class="container">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbar-info" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-bar"></span>
      <span class="navbar-toggler-bar"></span>
      <span class="navbar-toggler-bar"></span>
    </button>
    <%= link_to "دليل العالم", root_path, :class => "navbar-brand" %>
    <div class="collapse navbar-collapse" id="navbar-info" dir="rtl">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">

          <a class="nav-link" href="#">
            <i class="fa fa-home"></i>
            الرئيسية
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">
            <i class="fa fa-info"></i>
            حول الدليل
          </a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <i class="fa fa-user"></i>
            <% if user_signed_in? %>
              <%= current_user.username %>
            <% else %>
              حسابي
            <% end %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <% if user_signed_in? %>
              <%= link_to 'صفحتي الشخصية', current_user, class: "dropdown-item"%>
              <%= link_to('تسجيل الخروج', destroy_user_session_path, method: :delete, class: "dropdown-item") %>
              <%= link_to 'الاعدادت', edit_user_registration_path, class: "dropdown-item"%>

            <% else %>
              <%= link_to('تسجيل الدخول', new_user_session_path, class: "dropdown-item") %>
              <%= link_to('مستخدم جديد', new_user_registration_path, class: "dropdown-item") %>
            <% end %>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>
<br>
<%= render 'layouts/messages' %>
